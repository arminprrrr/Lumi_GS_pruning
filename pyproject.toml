[project]
name = "Lumi_GS_pruning"
version = "0.0.4"
description = "Training-time gaussian pruning and greyscale enforcement"
authors = [{name = "APR"}]
dependencies = []

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.4.2"
required_features = []